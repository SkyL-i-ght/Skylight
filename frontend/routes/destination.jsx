import React from "react";


export function Destination(props){
    return (
        <div>
            {/* props.restaurants, props.history, props.culture, props.activities, etc */}
        </div>
    )  
}